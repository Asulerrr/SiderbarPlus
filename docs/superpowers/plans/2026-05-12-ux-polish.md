# UX 优化：4 项改进实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 实现面板加载进度条、复制链接 Toast 反馈、拖拽手柄发光线、Favicon 重试上限 4 项 UX 改进。

**架构：** 进度条需要新增 IPC 通道（`panel:loading-state`）从主进程传递加载状态到 panel-chrome renderer；其余 3 项完全在 renderer 层处理，无需 IPC 改动。

**Tech Stack：** Electron IPC、React useState/useEffect、Tailwind CSS、TypeScript

---

## 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/shared/types.ts` | 修改 | 新增 `PanelLoadingPayload` 类型 |
| `src/shared/ipc-contracts.ts` | 修改 | 新增 `panelLoadingState` 通道 + `PanelAPI.onLoadingState` |
| `src/preload/panel.ts` | 修改 | 实现 `onLoadingState` IPC 监听 |
| `src/main/panels/WebPanelHost.ts` | 修改 | 监听 `did-start-loading` / `did-stop-loading`，调用 `emitLoading` |
| `src/main/panels/PanelManager.ts` | 修改 | 新增 `emitLoading` 方法，传入 `WebPanelHost` 依赖 |
| `src/renderer/panel-chrome/App.tsx` | 修改 | 进度条 state + 渲染 |
| `src/renderer/panel-chrome/panel.css` | 修改 | 进度条 CSS 动画 |
| `src/renderer/panel-menu/App.tsx` | 修改 | 复制链接 Toast state + 渲染 |
| `src/renderer/panel-chrome/components/ResizeHandle.tsx` | 修改 | hover state + 发光线渲染 |
| `src/renderer/dock/components/DockItem.tsx` | 修改 | 重试次数上限守卫 |

---

## Task 1：Favicon 重试次数上限

**文件：**
- 修改：`src/renderer/dock/components/DockItem.tsx:77-92`

- [ ] **步骤 1：在重试 useEffect 中添加上限守卫**

打开 `src/renderer/dock/components/DockItem.tsx`，找到第 77 行的 `useEffect`，在函数体第一行添加守卫：

```tsx
useEffect(() => {
  if (!iconFailed || panel.iconSource.kind !== 'auto' || !panel.web?.url) {
    return;
  }
  // 最多重试 5 次，之后保持字母 fallback
  if (retryTick >= 5) {
    return;
  }
  const delay = Math.min(5000 * Math.pow(2, retryTick), 40000);
  retryTimerRef.current = window.setTimeout(() => {
    setIconFailed(false);
    setRetryTick((t) => t + 1);
  }, delay);
  return () => {
    if (retryTimerRef.current) {
      window.clearTimeout(retryTimerRef.current);
      retryTimerRef.current = null;
    }
  };
}, [iconFailed, retryTick, panel.iconSource.kind, panel.web?.url]);
```

- [ ] **步骤 2：构建验证**

```bash
npm run build
```

预期：构建成功，无 TypeScript 错误。

- [ ] **步骤 3：提交**

```bash
git add src/renderer/dock/components/DockItem.tsx
git commit -m "fix: cap favicon retry at 5 attempts to prevent infinite requests"
```

---

## Task 2：Resize Handle 发光线

**文件：**
- 修改：`src/renderer/panel-chrome/components/ResizeHandle.tsx`

- [ ] **步骤 1：添加 hover state 和发光线渲染**

将 `src/renderer/panel-chrome/components/ResizeHandle.tsx` 完整替换为：

```tsx
import React, { useRef, useState } from 'react';
import type { Edge } from '@shared/types';

interface Props {
  edge: Edge;
}

const MIN_WIDTH = 320;

const clampToScreen = (width: number): number => {
  const max = Math.floor(Math.min(window.screen.availWidth, window.screen.width) * 0.8);
  return Math.min(Math.max(Math.round(width), MIN_WIDTH), max);
};

export const ResizeHandle: React.FC<Props> = ({ edge }) => {
  const activeRef = useRef(false);
  const rafRef = useRef<number | null>(null);
  const pendingWidthRef = useRef<number | null>(null);
  const [isHovered, setIsHovered] = useState(false);

  const flush = () => {
    rafRef.current = null;
    const w = pendingWidthRef.current;
    pendingWidthRef.current = null;
    if (w != null) window.panelAPI.resizeDrag(w);
  };

  const onMouseDown = (event: React.MouseEvent) => {
    if (activeRef.current) return;
    event.preventDefault();
    activeRef.current = true;

    const startX = event.screenX;
    const startWidth = window.innerWidth;

    const onMove = (ev: MouseEvent) => {
      const delta = edge === 'right' ? startX - ev.screenX : ev.screenX - startX;
      const next = clampToScreen(startWidth + delta);
      pendingWidthRef.current = next;
      if (rafRef.current == null) {
        rafRef.current = window.requestAnimationFrame(flush);
      }
    };

    const onUp = () => {
      window.removeEventListener('mousemove', onMove);
      window.removeEventListener('mouseup', onUp);
      if (rafRef.current != null) {
        window.cancelAnimationFrame(rafRef.current);
        rafRef.current = null;
      }
      const finalW = pendingWidthRef.current ?? clampToScreen(window.innerWidth);
      pendingWidthRef.current = null;
      activeRef.current = false;
      void window.panelAPI.commitResize(finalW);
    };

    window.addEventListener('mousemove', onMove);
    window.addEventListener('mouseup', onUp);
  };

  const positionStyle: React.CSSProperties =
    edge === 'right'
      ? { left: 0, top: 0, bottom: 0, width: 8 }
      : { right: 0, top: 0, bottom: 0, width: 8 };

  // 发光线贴近面板一侧：面板在右侧时手柄在左边缘，发光线贴右；面板在左侧时反之
  const glowStyle: React.CSSProperties =
    edge === 'right'
      ? { right: 0 }
      : { left: 0 };

  return (
    <div
      role="separator"
      aria-orientation="vertical"
      onMouseDown={onMouseDown}
      onMouseEnter={() => setIsHovered(true)}
      onMouseLeave={() => { if (!activeRef.current) setIsHovered(false); }}
      style={{ position: 'absolute', cursor: 'ew-resize', zIndex: 50, ...positionStyle }}
    >
      <div
        style={{
          position: 'absolute',
          top: 0,
          bottom: 0,
          width: 2,
          background: 'linear-gradient(180deg, transparent 0%, #6c63ff 30%, #6c63ff 70%, transparent 100%)',
          opacity: isHovered ? 0.7 : 0,
          transition: 'opacity 0.15s ease',
          pointerEvents: 'none',
          ...glowStyle
        }}
      />
    </div>
  );
};
```

- [ ] **步骤 2：构建验证**

```bash
npm run build
```

预期：构建成功，无 TypeScript 错误。

- [ ] **步骤 3：提交**

```bash
git add src/renderer/panel-chrome/components/ResizeHandle.tsx
git commit -m "feat: show glow line on resize handle hover"
```

---

## Task 3：新增加载状态 IPC 通道

**文件：**
- 修改：`src/shared/types.ts`
- 修改：`src/shared/ipc-contracts.ts`
- 修改：`src/preload/panel.ts`

- [ ] **步骤 1：在 types.ts 中新增 PanelLoadingPayload**

打开 `src/shared/types.ts`，在 `PanelNavigationPayload` 定义（第 112 行）之后添加：

```ts
export interface PanelLoadingPayload {
  panelId: string;
  isLoading: boolean;
}
```

- [ ] **步骤 2：在 ipc-contracts.ts 中新增通道和 API 方法**

打开 `src/shared/ipc-contracts.ts`：

1. 在 `IPC_CHANNELS` 对象的 `panelNavigationState` 行（第 78 行）之后添加：
```ts
panelLoadingState: 'panel:loading-state',
```

2. 在文件顶部 import 中添加 `PanelLoadingPayload`（已有 `PanelNavigationPayload` 的那行）：
```ts
import type {
  // ...existing imports...
  PanelLoadingPayload,
  PanelNavigationPayload,
  // ...
} from './types';
```

3. 在 `PanelAPI` 接口的 `onNavigationState` 行（第 139 行）之后添加：
```ts
onLoadingState: (callback: (payload: PanelLoadingPayload) => void) => () => void;
```

- [ ] **步骤 3：在 preload/panel.ts 中实现 onLoadingState**

打开 `src/preload/panel.ts`，在 `onNavigationState` 实现（第 59 行）之后添加：

```ts
onLoadingState: (callback) => {
  const listener = (
    _event: Electron.IpcRendererEvent,
    payload: Parameters<typeof callback>[0]
  ) => callback(payload);
  ipcRenderer.on(IPC_CHANNELS.panelLoadingState, listener);
  return () => ipcRenderer.removeListener(IPC_CHANNELS.panelLoadingState, listener);
},
```

- [ ] **步骤 4：构建验证**

```bash
npm run build
```

预期：构建成功，无 TypeScript 错误。

- [ ] **步骤 5：提交**

```bash
git add src/shared/types.ts src/shared/ipc-contracts.ts src/preload/panel.ts
git commit -m "feat: add panel:loading-state IPC channel"
```

---

## Task 4：主进程发送加载状态

**文件：**
- 修改：`src/main/panels/WebPanelHost.ts`
- 修改：`src/main/panels/PanelManager.ts`

- [ ] **步骤 1：在 WebPanelHost 依赖接口中添加 emitLoading**

打开 `src/main/panels/WebPanelHost.ts`，找到 `Dependencies` 接口（有 `emitNavigation` 的地方），添加：

```ts
emitLoading: (payload: PanelLoadingPayload) => void;
```

- [ ] **步骤 2：在 WebPanelHost 中监听 did-start-loading / did-stop-loading**

在 `view.webContents.on('did-navigate', ...)` 附近（第 474 行区域），在 `setupViewListeners` 方法中添加：

```ts
view.webContents.on('did-start-loading', () => {
  this.dependencies.emitLoading({ panelId, isLoading: true });
});

view.webContents.on('did-stop-loading', () => {
  this.dependencies.emitLoading({ panelId, isLoading: false });
});
```

- [ ] **步骤 3：在 PanelManager 中实现 emitLoading 并传入 WebPanelHost**

打开 `src/main/panels/PanelManager.ts`：

1. 在 `WebPanelHost` 构造调用（第 93 行）中添加 `emitLoading` 依赖：

```ts
this.webPanelHost = new WebPanelHost({
  readConfig: () => this.configStore.read(),
  updateConfig: (patch) => this.configStore.update(patch),
  emitNavigation: (payload) =>
    this.emitNavigation(payload.panelId, payload.url, payload.canGoBack),
  emitLoading: (payload) => this.emitLoading(payload.panelId, payload.isLoading)
});
```

2. 在 `emitNavigation` 方法（第 855 行）之后添加：

```ts
private emitLoading(panelId: string, isLoading: boolean): void {
  if (this.panelWindowRef.isDestroyed()) return;
  this.panelWindowRef.webContents.send(IPC_CHANNELS.panelLoadingState, {
    panelId,
    isLoading
  });
}
```

- [ ] **步骤 4：构建验证**

```bash
npm run build
```

预期：构建成功，无 TypeScript 错误。

- [ ] **步骤 5：提交**

```bash
git add src/main/panels/WebPanelHost.ts src/main/panels/PanelManager.ts
git commit -m "feat: emit panel loading state from main process"
```

---

## Task 5：面板加载进度条（renderer）

**文件：**
- 修改：`src/renderer/panel-chrome/App.tsx`
- 修改：`src/renderer/panel-chrome/panel.css`

- [ ] **步骤 1：在 panel.css 中添加进度条 CSS**

在 `src/renderer/panel-chrome/panel.css` 末尾追加：

```css
/* 面板加载进度条 */
.loading-bar {
  position: absolute;
  top: 0;
  left: 0;
  height: 2px;
  background: #6c63ff;
  transition: width 1.5s ease-out, opacity 0.2s ease;
  z-index: 200;
  pointer-events: none;
}
.loading-bar.loading-bar--done {
  opacity: 0;
}
```

- [ ] **步骤 2：在 App.tsx 中添加进度条 state**

打开 `src/renderer/panel-chrome/App.tsx`，在现有 `useState` 声明区域（`fading`、`contentSnapshotDataUrl` 等附近）添加：

```tsx
const [loadingWidth, setLoadingWidth] = useState(0);
const [loadingVisible, setLoadingVisible] = useState(false);
const [loadingDone, setLoadingDone] = useState(false);
const loadingTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
```

- [ ] **步骤 3：在 App.tsx 中订阅 onLoadingState**

在订阅 `onNavigationState` 的 `useEffect` 附近，添加新的 `useEffect`：

```tsx
useEffect(() => {
  return window.panelAPI.onLoadingState((payload) => {
    if (payload.panelId !== chromeState.panelId) return;

    if (payload.isLoading) {
      // 清除上次的淡出计时器
      if (loadingTimerRef.current) clearTimeout(loadingTimerRef.current);
      setLoadingDone(false);
      setLoadingVisible(true);
      setLoadingWidth(70);
    } else {
      // 跳到 100%，然后淡出
      setLoadingWidth(100);
      loadingTimerRef.current = setTimeout(() => {
        setLoadingDone(true);
        loadingTimerRef.current = setTimeout(() => {
          setLoadingVisible(false);
          setLoadingWidth(0);
          setLoadingDone(false);
        }, 200);
      }, 100);
    }
  });
}, [chromeState.panelId]);
```

- [ ] **步骤 4：在 App.tsx 中渲染进度条**

找到 header `<div className="flex h-[76px] shrink-0 ...">` 的父容器（第 589 行附近的 `<div style={{ position: 'absolute', ... }}>` 内），在 header div 之前插入进度条：

```tsx
{loadingVisible ? (
  <div
    className={`loading-bar${loadingDone ? ' loading-bar--done' : ''}`}
    style={{ width: `${loadingWidth}%` }}
  />
) : null}
```

- [ ] **步骤 5：构建验证**

```bash
npm run build
```

预期：构建成功，无 TypeScript 错误。

- [ ] **步骤 6：提交**

```bash
git add src/renderer/panel-chrome/App.tsx src/renderer/panel-chrome/panel.css
git commit -m "feat: add loading progress bar to panel chrome"
```

---

## Task 6：复制链接 Toast（panel-menu）

**文件：**
- 修改：`src/renderer/panel-menu/App.tsx`

- [ ] **步骤 1：添加 Toast state 和渲染**

将 `src/renderer/panel-menu/App.tsx` 中的 `handleAction` 函数和 return 部分修改如下：

在 `useState` 声明区域添加：
```tsx
const [showCopyToast, setShowCopyToast] = useState(false);
const copyToastTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
```

在文件顶部 import 中添加 `useRef`（已有 `useEffect, useState`）：
```tsx
import { useEffect, useRef, useState } from 'react';
```

在 `handleAction` 函数中，找到 `copy-link` 的处理逻辑（第 58-65 行），修改为：

```tsx
if (result.ok) {
  setState(result.data);
  if (action === 'copy-link') {
    // 显示 Toast，2s 后消失，然后关闭菜单
    if (copyToastTimerRef.current) clearTimeout(copyToastTimerRef.current);
    setShowCopyToast(true);
    copyToastTimerRef.current = setTimeout(async () => {
      setShowCopyToast(false);
      await window.panelMenuAPI.closeMenuAndResumeHover();
    }, 2000);
    return;
  }
  if (
    action === 'reload' ||
    action === 'open-edit-site' ||
    action === 'clear-site-data' ||
    action === 'open-site-info'
  ) {
    await window.panelMenuAPI.closeMenuAndResumeHover();
  }
}
```

在 return 的 JSX 中，在 `<main>` 内最顶层添加 Toast：

```tsx
return (
  <main
    className="min-h-screen bg-transparent p-0"
    onMouseLeave={() => {
      void window.panelMenuAPI.closeMenu();
    }}
  >
    {showCopyToast ? (
      <div
        style={{
          position: 'fixed',
          top: 8,
          left: '50%',
          transform: 'translateX(-50%)',
          background: '#4ade80',
          color: '#000',
          fontSize: 12,
          fontWeight: 600,
          padding: '4px 14px',
          borderRadius: 20,
          zIndex: 1000,
          whiteSpace: 'nowrap',
          pointerEvents: 'none'
        }}
      >
        ✓ 链接已复制
      </div>
    ) : null}
    <div className="rounded-lg border border-white/8 bg-[#2D2D2D] p-1 shadow-[0_10px_30px_rgba(0,0,0,0.35)]">
    {/* 以下按钮与原文件完全一致，不需要修改 */}
      <button
        type="button"
        className="flex h-8 w-full items-center gap-2 rounded-md px-3 text-left text-sm text-white transition-colors hover:bg-white/30"
        onClick={() => void handleAction('reload')}
      >
        <RotateCw size={14} className="text-white/72" />
        <span>刷新</span>
      </button>
      <button
        type="button"
        className="flex h-8 w-full items-center gap-2 rounded-md px-3 text-left text-sm text-white transition-colors hover:bg-white/30"
        onClick={() => void handleAction('copy-link')}
      >
        <LinkIcon size={14} className="text-white/72" />
        <span>复制链接</span>
      </button>
      <button
        type="button"
        className="flex h-8 w-full items-center gap-2 rounded-md px-3 text-left text-sm text-white transition-colors hover:bg-white/30"
        onClick={() => void handleAction('toggle-mobile-view')}
      >
        <Smartphone size={14} className="text-white/72" />
        <span className="flex-1">显示移动视图</span>
        {state?.userAgentMode === 'mobile' ? <Check size={14} className="text-white/72" /> : null}
      </button>
      <button
        type="button"
        className="flex h-8 w-full items-center gap-2 rounded-md px-3 text-left text-sm text-white transition-colors hover:bg-white/30"
        onClick={() => void handleAction('toggle-notifications-snooze')}
      >
        <BellOff size={14} className="text-white/72" />
        <span className="flex-1">{state?.notificationsSnoozed ? '取消推迟通知' : '推迟通知'}</span>
        {state?.notificationsSnoozed ? <Check size={14} className="text-white/72" /> : null}
      </button>
      <div className="my-1 h-px bg-white/8" />
      <button
        type="button"
        className="flex h-8 w-full items-center gap-2 rounded-md px-3 text-left text-sm text-white transition-colors hover:bg-white/30"
        onClick={() => void handleAction('open-edit-site')}
      >
        <Pencil size={14} className="text-white/72" />
        <span>编辑此站点</span>
      </button>
      <button
        type="button"
        className="flex h-8 w-full items-center gap-2 rounded-md px-3 text-left text-sm text-white transition-colors hover:bg-white/30"
        onClick={() => void handleAction('clear-site-data')}
      >
        <Trash2 size={14} className="text-white/72" />
        <span>清除此站点数据</span>
      </button>
      <button
        type="button"
        className="flex h-8 w-full items-center gap-2 rounded-md px-3 text-left text-sm text-white transition-colors hover:bg-white/30"
        onClick={() => void handleAction('open-site-info')}
      >
        <Info size={14} className="text-white/72" />
        <span>站点信息</span>
      </button>
    </div>
  </main>
);
```

- [ ] **步骤 2：构建验证**

```bash
npm run build
```

预期：构建成功，无 TypeScript 错误。

- [ ] **步骤 3：提交**

```bash
git add src/renderer/panel-menu/App.tsx
git commit -m "feat: show copy toast feedback in panel menu"
```

---

## Task 7：推送到 GitHub

- [ ] **步骤 1：推送所有提交**

```bash
git push
```

预期：所有 6 个新提交推送成功。
