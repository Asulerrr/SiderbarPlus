# SideBar

Windows 桌面侧边栏应用。基于 Electron + React + TypeScript + Tailwind。

## 开发

```bash
npm install
npm run dev        # 开发模式
npm run build      # 编译
npm run build:win  # 编译 + 打包 Windows 安装包
npm test           # 运行测试
```

## 构建

```bash
npm run build:win
```

产物在 `dist/` 目录。
